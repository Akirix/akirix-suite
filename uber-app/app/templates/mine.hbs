<div class="navbar-secondary" role="navigation">
    <ul class="list-h">
        <li>
            {{#link-to 'mine.tickets'}}
                Tickets
                {{#if itemCounts.tickets.ticketsUser}}
                    <span class="badge badge-second-menu">
                        {{itemCounts.tickets.ticketsUser}}
                    </span>
                {{/if}}
            {{/link-to}}
        </li>
        <li>
            {{#link-to 'mine.uber-tasks'}}
                Tasks
                {{#if itemCounts.uberTasks.dueUser}}
                    <span class="badge badge-second-menu">
                        {{itemCounts.uberTasks.dueUser}}
                    </span>
                {{/if}}
            {{/link-to}}
        </li>
    </ul>
</div>
<div class="viewport viewport-full viewport-content-with-nav">
    <div class="viewport-t-pane-primary viewport-t-pane-primary-with-toolbar scroll-y">
        <div class="pane-primary">
            <div class="pane-primary-content">
                {{outlet 'panePrimary'}}
            </div>
        </div>
    </div>
    <div class="viewport-right pane pane-secondary">
        {{outlet 'paneSecondary'}}
    </div>
</div>